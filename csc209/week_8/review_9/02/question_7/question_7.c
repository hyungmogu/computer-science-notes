#include <stdio.h>
#include <stdlib.h>

int swap_bytes(int i);

int main() {
    printf("rotated left: %hx", rotate_left(0x12345678));
    return 0;
}

int rotate_left(int i) {
    int res;

    return res;
}